import { MutationResolvers } from "../../../../graphql/generated";

export const {{mutationName}}Mutation: MutationResolvers = {
{{mutationName}}: (parent, args, context) => {

return context.{{moduleName}}.mutate{{mutationName}}({{#if hasArguments}}args{{/if}})},
};
