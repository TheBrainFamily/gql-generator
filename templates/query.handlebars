import { QueryResolvers } from "../../../../graphql/generated";

export const {{queryName}}Query: QueryResolvers = {
    {{queryName}}: (parent, args, context) => {
    return context.{{moduleName}}.query{{queryName}}({{#if hasArguments}}args{{/if}})
},
};
